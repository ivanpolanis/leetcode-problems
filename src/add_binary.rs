//TODO:
impl Solution {
  pub fn add_binary(a: String, b: String) -> String {
      let mut lena = a.len();
      let mut lenb = b.len();
      let mut carry:bool = false;
      while lena != 0 | lenb !=0 | !carry {
        
      }
  }
}